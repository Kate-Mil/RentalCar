import { FavoriteList } from "components/FavotiteList/FavoriteList";
import { Section } from "../../components/Common.styled";

export const Favorites = () => {
  return (
    <Section>
      <FavoriteList />
    </Section>
  );
};
