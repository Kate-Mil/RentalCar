import { HomePage } from "components/HomePage/HomePage";
import { Section } from "components/Common.styled";

export const Home = () => {
  return (
    <Section>
      <HomePage />
    </Section>
  );
};
