import { AppBar } from "components/AppBar/AppBar";
import { HeaderContainer } from "./Header.styled";

export const Header = () => {
  return (
    <HeaderContainer>
      <AppBar />
    </HeaderContainer>
  );
};
