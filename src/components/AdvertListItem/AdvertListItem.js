export const AdvertListItem = ({ data }) => {
  const {
    // year,
    make,
    // model,
    // type,
    // img,
    // functionalities,
    // rentalPrice,
    // rentalCompany,
    // address,
    // mileage,
  } = data;
  return <div>{make}</div>;
};
